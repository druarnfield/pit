[project]
name = "pit-sdk"
version = "0.1.0"
description = "Python SDK client for the Pit orchestrator"
requires-python = ">=3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel.force-include]
"pit" = "pit"
